<UserControl x:Class="HSLCopy.Usc.TcpIpControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HSLCopy.Usc"
             mc:Ignorable="d" 
             xmlns:vm="clr-namespace:HSLCopy.ViewModels" 
             d:DataContext="{d:DesignInstance vm:MainViewModel}" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"  
             xmlns:hc="https://handyorg.github.io/handycontrol"
             >
    <Grid>
        <StackPanel Orientation="Horizontal">
            <TextBlock Margin="15,0,5,0" Text="Ip地址:" FontSize="13" VerticalAlignment="Center"/>
            <TextBox Text="{Binding IP}" Margin="5,0,5,0" Height="25" Width="150" Style="{StaticResource MaterialDesignTextBoxBase}" Background="White" FontSize="13" VerticalAlignment="Center"/>
            <TextBlock Margin="10,0,5,0" Text="端口号:" FontSize="13" VerticalAlignment="Center"/>
            <TextBox Text="5050" Margin="5,0,5,0" Height="25" Width="50" Style="{StaticResource MaterialDesignTextBoxBase}" Background="White" FontSize="13" VerticalAlignment="Center"/>
            <TextBlock Margin="10,0,5,0" Text="连接超时:" FontSize="13" VerticalAlignment="Center"/>
            <TextBox Text="{Binding IP}" Margin="5,0,5,0" Height="25" Width="65" Style="{StaticResource MaterialDesignTextBoxBase}" Background="White" FontSize="13" VerticalAlignment="Center"/>
            <TextBlock Margin="10,0,5,0" Text="接收超时:" FontSize="13" VerticalAlignment="Center"/>
            <TextBox Text="{Binding IP}" Margin="5,0,5,0" Height="25" Width="65" Style="{StaticResource MaterialDesignTextBoxBase}" Background="White" FontSize="13" VerticalAlignment="Center"/>
            <Button Content="more"/>

        </StackPanel>
    </Grid>
</UserControl>
